# 1. 모든 파일의 깃허브 행렬 차단
*

# 2. 예외적으로 '핵심 요원'만 통과 허용 (앞에 !를 붙임)
!.gitignore
!SOXL_sigma_alert.py
!config.json
!requirements.txt
!README.md
!.github/

# 3. (추가 팁) .env 파일에 민감한 정보(API 키 등)가 있다면 
# 보안을 위해 깃허브에 올리지 않는 것이 원칙입니다.
# 만약 깃허브에 API 키가 노출되는 게 걱정되신다면 
# 위의 !.env 줄만 지우시면 됩니다.