스윙_투_리뷰

목적
- 이번 브랜치(agents/swing-bot-bug-validation)에서 수행된 방어적 수정, 백테스트 하드닝, TP/TF 최적화 실험 결과를 요약하고 리뷰 포인트를 정리한다.

요약(핵심)
- 코드 변경
  - swing_bot.py: 런타임 방어적 수정(응답 검증, 원자적 상태저장, DRY_RUN 개선, Discord 예외 안전화 등)
  - swing_bot_backtest.py: yfinance 안정성 보강(칼럼/타임존 표준화, 체인 페치), --regime-tf 및 --allow-first-entry 옵션 추가
- 실험 결과 요약
  - TQQQ: TAKE_PROFIT = entry_ATR × 1.5  (5년 백테스트: 13회 → 연 2.6회)
  - SOXL: TAKE_PROFIT = entry_ATR × 3.5  (5년 백테스트: 36회 → 연 7.2회)
- 실험 산출물(grid_results/, parse_backtest_results.py 등)은 사용자의 승인으로 삭제되어 현재 로컬에 없음. 필요 시 재실행으로 재생산 가능.

리뷰 포인트(권장 검토 항목)
1. 설정 반영
   - swing_config.json에 위 TP 값(종목별)을 반영할지 결정 필요
2. 운영 전 DRY_RUN 기간
   - 권장: DRY_RUN 모드로 1~4주 모니터링(브리핑 형식·발생 빈도·에러 로그 확인)
   - 모니터링에서 발견된 작은 문제(예: Discord rate limit, API 응답 일부 변형)는 운영 전 수정 권장
3. 문서 반영
   - STRATEGY_RULES.md: 4시간 TF 철학·LOC↔ATH_DCA 규칙 및 비상 종료 조건 등 주요 규칙은 최신. 현재 변경 사항(방어적 수정 내역과 TP 권고)는 별도의 요약 문서에 포함되어야 함
   - TRIGGER_OPTIMIZATION_SUMMARY.md: (본 파일과 연계) 트리거 권고는 유효. 2026-08-06 TP 최적화 요약을 병기함
4. 아티팩트/백업 정책
   - 향후 대규모 실험 전에는 결과를 압축하여 별도 원격(예: S3 또는 내부 NAS)에 보관 권장
5. 테스트
   - py_compile 확인 (이미 통과됨)
   - 권장 추가: DRY_RUN에서 실제 브리핑 발송(읽기 전용) 후 포맷 및 알림 레이턴시 점검

권장 다음 단계(사용자 승인 필요)
1. swing_config.json에 TP 값 반영 후 PR 생성(제가 PR 초안 생성 가능)
2. DRY_RUN 스케줄링(기간/대상 종목/알림 채널을 지정해 주세요)
3. 추가 문서(한 페이지 요약)를 README 또는 STRATEGY_RULES.md에 병기(제가 초안 작성 가능)

파일·커밋 참고
- 브랜치: agents/swing-bot-bug-validation
- 관련 파일: swing_bot.py, swing_bot_backtest.py, swing_config.json, STRATEGY_RULES.md, TRIGGER_OPTIMIZATION_SUMMARY.md

마무리
- 어떤 액션(예: TP 반영 PR 생성, DRY_RUN 시작, 실험 로그 복구)을 먼저 진행할지 알려주시면 바로 처리하겠습니다.

(작성자: Copilot CLI 런타임을 사용하는 AI 어시스턴트)