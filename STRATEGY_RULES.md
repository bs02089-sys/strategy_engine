# 📋 전략 규칙 — 순수 LOC 지정가 20분할 DCA

> 기준: `LOC_DCA_strategy.py` + `portfolio_config.json` (2026-08-16) | 포트폴리오: TQQQ
>
> ⚠️ 2026-08-16 단일 논리 재구성: 이동평균선(MA 레짐/MA 정렬) · RSI+볼륨 ·
> ATH 하락분할 DCA(비상 모드) · STAGE5 · 회복 재진입을 **전부 삭제**했다.
> 로직을 섞는 방식은 효율이 낮고 오버피팅 문제가 있어, **하나의 논리**만 쓴다.

## 1. 변동성(σ) 계산 — 유일한 매수가 기준
- σ = 로그수익률 분산 **EWMA 재귀** (λ **0.94**, 룩백 **252일**) → 일일 σ
- **90일마다 또는 설정 변경 시** 자동 재계산 (이력: `sigma_history.csv`)

## 2. 매일 실행 순서 (월~금 23:30 UTC = NY 19:30)
1. 시그마 갱신 판정 → 2. 로테이션 만기 점검 → 3. **LOC 체결 감지** → 4. 브리핑 작성 → 5. Discord 발송

## 3. LOC 매수 규칙 — 유일한 매수 논리 💰
- **LOC 매수가** = 전일 종가 × (1 − σ × `ENTRY_MULTIPLIER`) (기본 승수 1.1)
- **당일 저가 ≤ LOC** → **1차 체결** ($2,500 × 최대 **20차**)
- 체결 상태는 `LOC_DCA_USED_SPLITS`(체결일 목록)에 영속화 — 같은 날 중복 감지 방지,
  `LOC_DCA` 설정(SPLITS/BUY_AMOUNT) 변경 시 자동 리셋
- **20차 전부 소진 → 매수 중단** (신규 신호 없음). 수동 재개는 `--reset-splits`

## 4. 매도 규칙 — 없음
- 순수 **적립 전용** (매수만). 매도/현금 전환 로직을 두지 않는다 — MA 레짐 필터가
  유일한 매도 규칙이었으나 단일 논리 원칙에 따라 삭제됨

## 5. 실행 주의사항
- 실전 엔진의 모든 규칙은 브리핑의 **▶ 실행 액션 라인(신호)**이며, **실제 체결은 사용자 수동 매매**
- 매수 신호는 시그널이지 보장 수익이 아님
- 설정값(승수/분할 수/차수당 금액)은 코드에 하드코딩하지 않고 `portfolio_config.json`의
  `LOC_DCA` 블록에서 읽는다 (단일 소스)
