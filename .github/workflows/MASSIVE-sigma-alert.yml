name: Daily Sigma Alert (MASSIVE)

on:
  schedule:
    - cron: '0 10 * * 1-5'   # 평일 오전 10시 UTC (KST 오후 7시)
  workflow_dispatch:

jobs:
  run-sigma-alert:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Run Sigma Report
        run: python SOXL_sigma_alert.py

      - name: Push updated config.json
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add config.json
          git commit -m "Auto update config.json after market close"
          git push
