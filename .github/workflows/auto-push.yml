name: auto push alert script

on:
  schedule:
    - cron: "0 10 * * 1-5"   # 평일 UTC 10시 → KST 오후 7시
  workflow_dispatch:

jobs:
  run-alert:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - uses: actions/setup-python@v4
        with:
          python-version: "3.12"

      - run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      # 수동 실행 시에도 오후 7시까지 대기
      - run: |
          TARGET=$(date -d "today 10:00 UTC" +%s)
          NOW=$(date +%s)
          SLEEP=$((TARGET - NOW))
          [ $SLEEP -gt 0 ] && sleep $SLEEP

      - run: python TQQQ_SOXL_QLD_2sigma_alert.py

      - run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add .
          git diff --cached --quiet || git commit -m "Auto update alert script" && git push
